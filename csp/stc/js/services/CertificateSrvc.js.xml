<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="25">
<CSP name="stc/js/services/CertificateSrvc.js" application="/csp/four/" default="1"><![CDATA[
'use strict';
//dddddddв

/*===========================================================================================
Сертификаты
===========================================================================================*/

servicesModule.factory('CertificateSrvc', function(DALSrvc) {
	
	return {
	      /* Все сертификаты */
        getCertificates: function(pageCurr, pageSize, sqlName, isDown, searchSqlName, searchText, isPrinted){
            var first = pageSize * (pageCurr - 1) + 1;
            var obj = {sqlName: sqlName, 
                       isDown: isDown, 
                       first: first, 
                       last: first + pageSize - 1,
                       searchSqlName: searchSqlName, 
                       searchText: searchText,
                       isPrinted: isPrinted};

            return DALSrvc.getPromise('save', StcAppSetting.admin + '/json/certificates', obj);
        },
        print: function(number){
            return DALSrvc.getPromise('save', StcAppSetting.admin + '/json/printCertificate/' + number, null);
        }
    }
});
  
]]></CSP>
</Export>
